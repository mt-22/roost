use ratatui::Frame;

use crate::tui::main_view::state::MainViewState;

/// Render the complete main view into the given frame.
pub fn render(_state: &mut MainViewState, _frame: &mut Frame) {
    // TODO: implement in Stream 2 (ui.rs)
}
