# TODO

## High priority

- [ ] **Main TUI** — `roost` with no args currently prints a placeholder. Build a landing TUI showing active profile, app count, recent changes, with keyboard shortcuts to common actions.

- [ ] **Tests** — Add test coverage for `list`, `save`, `where --profile`, and styling changes. Dev-deps (`assert_cmd`, `predicates`, `tempfile`) are already in place.


